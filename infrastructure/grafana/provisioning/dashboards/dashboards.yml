apiVersion: 1

providers:
  - name: 'Task Platform Dashboards'
    orgId: 1
    folder: 'Task Platform'
    folderUid: 'task-platform'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
